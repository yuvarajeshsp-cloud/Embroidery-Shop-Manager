import type { CapacitorConfig } from "@capacitor/cli"

const config: CapacitorConfig = {
  appId: "com.paavaiembroidery.shopmanager",
  appName: "Paavai Embroidery",
  webDir: "dist",
}

export default config
